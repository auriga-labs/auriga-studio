# Auriga Studio — CLAUDE.md

## プロジェクト概要

**Auriga Studio** はブラウザ(Electron)で動作する統合型動画編集ソフトです。
YMM4・DaVinci Resolve・Adobe Premiere のプロジェクトファイルを橋渡しし、クラウド連携も提供します。

- ターゲット: Electron 42.2.0 / Chromium 148 / Node.js 24.15.0
- 対象 OS: Windows (x64)

---

## 技術スタック

| 項目 | 内容 |
|------|------|
| 言語 | Vanilla JavaScript（TypeScript **なし**） |
| ランタイム | Electron 42.2.0 |
| 認証 | Google OAuth（未実装） |
| ストレージ | ときくんくらうど！（無料 15 GB 予定） |

---

## リポジトリ構成

```
/
├── main.js          # Electron メインプロセス
├── index.html       # レンダラーエントリポイント
├── PROJECT.md       # プロジェクト仕様書
├── CLAUDE.md        # このファイル
└── ...
```

> ⚠️ 新しいファイルを追加するときは、まずルートの構成に合わせて配置先を検討すること。
> 大きな機能単位は `src/` 以下に整理していくことを推奨するが、既存ファイルの配置は勝手に変えない。

---

## コーディングルール

### 言語・スタイル

- **Vanilla JS のみ**。TypeScript・トランスパイラ・バンドラーを勝手に導入しない。
- **コメントはすべて日本語**で書く。
  ```js
  // ✅ タイムラインの描画を初期化する
  function initTimeline() { ... }

  // ❌ Initialize the timeline rendering
  function initTimeline() { ... }
  ```
- セミコロンあり、インデント 2 スペース（既存コードに合わせる）。
- `var` は使わない。`const` / `let` を使う。

### ファイル分割

- 1 ファイルに 1 つの責務を持たせる。
- 機能が増えたら `src/` に切り出すが、事前に方針を確認すること。

---

## Git ルール

**毎ターン（タスク完了ごと）に必ず commit & push すること。**

```bash
git add -A
git commit -m "feat: ○○を実装"
git push
```

### コミットメッセージの形式

```
<type>: <日本語で変更内容を簡潔に>
```

| type | 用途 |
|------|------|
| `feat` | 新機能 |
| `fix` | バグ修正 |
| `refactor` | 動作変更なしのリファクタ |
| `docs` | ドキュメントのみの変更 |
| `chore` | ビルド・設定変更など |

---

## 対応ソフトウェア

### サポート済み（目標）

| ソフト | バージョン | 優先度 |
|--------|-----------|--------|
| YMM4 | v4.53.0.9 | 高 |
| DaVinci Resolve | 21.0.1 | 高 |
| Adobe Premiere | 26.3.0 (Build 93) | 高 |

### YMM4 ファイル仕様メモ

- `.ymmp` は単一の JSON ファイル。
- 読み込み・書き出しを実装予定。構造解析が必要な場合はサンプルファイルを参照すること。

### Premiere Pro

- プロジェクト形式の仕様が不明なため、現時点では対応を保留。
- 無理に実装しようとしない。

---

## 実装済み・予定機能

### テーマ

- YMM4 / DaVinci / Premiere / ときくんオリジナル の 4 種類を切り替え可能にする。

### 認証（未実装）

- Google OAuth でログイン・ログアウト。
- 実装時は OAuth クライアント ID をハードコードせず、環境変数または設定ファイルから読む。

### ときくんくらうど！（未実装）

- 無料 15 GB を提供予定。
- スケーリングは慎重に設計すること。コスト試算なしに大きな変更を加えない。

---

## やってはいけないこと

- TypeScript・Webpack・Vite などのビルドツールを無断で導入する。
- `var` を使う。
- コメントを英語で書く。
- タスク完了後に commit & push を忘れる。
- Premiere Pro の仕様が不明なまま無理に実装しようとする。
- ときくんくらうど！のストレージ設計をスケーリング計画なしに変更する。
- 既存ファイルのディレクトリ配置を勝手に変える。

---

## よく使うコマンド

```bash
# Electron を起動して動作確認
npx electron .

# Git: 変更をコミット & プッシュ（毎ターン必須）
git add -A && git commit -m "feat: ○○" && git push
```